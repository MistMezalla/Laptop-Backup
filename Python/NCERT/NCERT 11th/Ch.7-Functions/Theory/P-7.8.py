def fullname(fname,lname):
    print(fname+lname) #the output won't keep any space between the fisrt and last names
    print(fname+' '+lname) #the output will keep space as sepcified bet first and last name

f_name=input('Enter your first name: ')
l_name=input('Enter your last name: ')

fullname(f_name,l_name)
